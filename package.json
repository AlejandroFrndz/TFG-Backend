{
    "name": "backend",
    "version": "1.0.0",
    "description": "",
    "main": "index.ts",
    "scripts": {
        "dev": "set NODE_ENV=dev&& ts-node src/index.ts",
        "devUnix": "NODE_ENV=dev && ts-node src/index.ts",
        "dev:watch": "nodemon --watch './' --ignore './tests' --ignore './build' --ignore './node_modules' --ext 'ts,json,js' --exec npm run dev",
        "devUnix:watch": "nodemon --watch './' --ignore './tests' --ignore './build' --ignore './node_modules' --ext 'ts,json,js' --exec npm run devUnix",
        "docker:up": "docker-compose up -d",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "author": "alexFrndz",
    "license": "ISC",
    "devDependencies": {
        "@types/express": "^4.17.13",
        "@types/node": "^17.0.21",
        "nodemon": "^2.0.15",
        "prettier": "^2.6.0",
        "ts-node": "^10.7.0",
        "typescript": "^4.6.2"
    },
    "dependencies": {
        "dotenv": "^16.0.0",
        "express": "^4.17.3"
    }
}
